export class Constructor {
  constructor(string, number, object) {
    this.string = string
    this.number = number
    this.object = object
  }

  cappinProto() {
    if ((this.string === "strings and numbers like") && (this.number === 5) && (this.object === [empty arrays])) {
      return "The same exact result in test and Constructor pages";
    }
  }
}
